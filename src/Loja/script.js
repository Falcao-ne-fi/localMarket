let produtos

window.onload = function(){
    var storedUser = localStorage.getItem("usario")
    var user = JSON.parse(storedUser)
    var dataEntrada = new Date(user.dataEntrada)

    let produtos = "coco"
    
}